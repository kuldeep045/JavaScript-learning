const lang = {
    cpp : "C++",
    js : "Javascript",
    py : "python"

}

// for (const key in lang) {
//    console.log(`Key is ${key} and value is ${lang[key]}`)
// }
const langArr = ["java", "js", "ruby"]
for (const key in langArr) {
    console.log(`Key is ${key} and value is ${langArr[key]}`)
 }
